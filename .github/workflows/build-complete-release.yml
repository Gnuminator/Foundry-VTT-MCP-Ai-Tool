name: Build Complete Release

# Canonical release workflow for Foundry AI Tool. Builds the Windows NSIS installer,
# standalone MCP server ZIP, and Foundry module ZIP, then publishes a GitHub
# Release and updates the Foundry VTT Package Registry. Trigger manually via
# workflow_dispatch or automatically on tag pushes (the create-release job is gated on a v* tag ref).
on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version tag (leave empty to auto-detect from package.json)'
        required: false
        default: ''
      dry_run_foundry:
        description: 'Test Foundry API call without actually updating registry'
        required: false
        default: 'false'
        type: boolean

jobs:
  build-nsis:
    runs-on: windows-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v6
      
    - name: Setup Node.js
      uses: actions/setup-node@v6
      with:
        node-version: '20.19'
        
    - name: Set Package Version
      id: version
      run: |
        try {
          if ("${{ github.event.inputs.version }}" -ne "") {
            $version = "${{ github.event.inputs.version }}"
            Write-Host "✓ Using manual input version: $version"
            
            # Validate version format
            if ($version -notmatch '^v?\d+\.\d+\.\d+') {
              throw "Invalid version format: '$version'. Expected format: v0.0.0 or 0.0.0"
            }
          } elseif ("${{ github.ref_type }}" -eq "tag") {
            $version = "${{ github.ref_name }}"
            Write-Host "✓ Using tag version: $version"
            
            # Validate tag version format
            if ($version -notmatch '^v?\d+\.\d+\.\d+') {
              throw "Invalid tag version format: '$version'. Expected format: v0.0.0"
            }
          } else {
            Write-Host "📋 Reading version from package.json..."
            
            # Check if package.json exists
            if (-not (Test-Path "package.json")) {
              throw "package.json not found in repository root"
            }
            
            # Read and parse package.json
            $packageJson = Get-Content package.json -Raw | ConvertFrom-Json
            
            # Validate version field exists
            if (-not $packageJson.version) {
              throw "No 'version' field found in package.json"
            }
            
            $packageVersion = $packageJson.version
            Write-Host "📦 Found package.json version: $packageVersion"
            
            # Validate version format
            if ($packageVersion -notmatch '^\d+\.\d+\.\d+') {
              throw "Invalid package.json version format: '$packageVersion'. Expected format: 0.0.0"
            }
            
            $version = "v$packageVersion"
            Write-Host "✓ Using package.json version: $version"
          }
          
          # Final validation - ensure no invalid characters for filename
          if ($version -match '[<>:"/\\|?*]') {
            throw "Version contains invalid filename characters: '$version'"
          }
          
          Write-Host "🎯 Final PACKAGE_VERSION: $version"
          echo "PACKAGE_VERSION=$version" >> $env:GITHUB_ENV
          echo "version=$version" >> $env:GITHUB_OUTPUT
          
        } catch {
          Write-Error "❌ Failed to determine package version: $($_.Exception.Message)"
          Write-Error "📋 Context: github.event.inputs.version='${{ github.event.inputs.version }}', github.ref_type='${{ github.ref_type }}', github.ref_name='${{ github.ref_name }}'"
          exit 1
        }
        
    - name: Install workspace dependencies (CI)
      run: npm ci --workspaces --include-workspace-root
      
    - name: Build shared types
      run: npm run build --workspace=shared
        
    - name: Build MCP Server (Bundled)
      run: npm run build:bundle --workspace=packages/mcp-server
        
    - name: Setup NSIS
      run: choco install nsis -y

    - name: Build NSIS Installer
      run: |
        $env:PATH = "C:\Program Files (x86)\NSIS;$env:PATH"
        cd installer
        node build-nsis.js --version $env:PACKAGE_VERSION
        
    - name: Create Foundry Module ZIP
      run: |
        Write-Host "📦 Creating Foundry module ZIP for registry..."
        cd packages/foundry-module
        
        # Create ZIP excluding hidden files and node_modules
        $compress = @{
          Path = Get-ChildItem -Path "." -Exclude ".*", "node_modules"
          DestinationPath = "..\..\foundry-mcp-bridge.zip"
          CompressionLevel = "Optimal"
        }
        Compress-Archive @compress -Force
        
        # Verify ZIP was created
        if (Test-Path "..\..\foundry-mcp-bridge.zip") {
          $size = (Get-Item "..\..\foundry-mcp-bridge.zip").Length / 1KB
          Write-Host "✓ Foundry module ZIP created ($([math]::Round($size, 1)) KB)"
        } else {
          Write-Host "❌ Foundry module ZIP creation failed"
          exit 1
        }
        
    - name: Create Standalone MCP Server ZIP
      run: |
        Write-Host "📦 Creating standalone MCP server ZIP for manual installation..."
        
        # Create a temporary directory for the standalone package
        $standalonePath = "standalone-mcp-server"
        New-Item -ItemType Directory -Force -Path $standalonePath
        
        # Copy MCP server bundle
        Copy-Item "packages\mcp-server\dist\index.bundle.cjs" "$standalonePath\index.js"
        
        # Create package.json for standalone
        $standalonePackage = @{
          name = "foundry-mcp-server"
          version = "${{ env.PACKAGE_VERSION }}".TrimStart('v')
          description = "Foundry VTT MCP Server - Standalone Installation"
          main = "index.js"
          scripts = @{
            start = "node index.js"
          }
          engines = @{
            node = ">=18.0.0"
          }
        } | ConvertTo-Json -Depth 3
        
        $standalonePackage | Out-File -FilePath "$standalonePath\package.json" -Encoding UTF8
        
        # Create README for standalone
        $readmeLines = @(
          "# Foundry MCP Server - Manual Installation",
          "",
          "This package contains the standalone MCP server for manual installation on any platform.",
          "",
          "## Requirements",
          "- Node.js 18.0.0 or later",
          "- Foundry VTT with MCP Bridge module installed",
          "",
          "## Installation",
          "1. Extract this ZIP to your desired location",
          "2. Run: npm start or node index.js",
          "3. Configure Claude Desktop to connect to this server",
          "4. Install the MCP Bridge module in Foundry VTT",
          "",
          "For full documentation visit: https://github.com/Gnuminator/Foundry-VTT-MCP-Ai-Tool",
          "",
          "Version: ${{ env.PACKAGE_VERSION }}"
        )
        $readmeLines | Out-File -FilePath "$standalonePath\README.md" -Encoding UTF8
        
        # Create the ZIP
        $compress = @{
          Path = $standalonePath
          DestinationPath = "foundry-mcp-server-${{ env.PACKAGE_VERSION }}.zip"
          CompressionLevel = "Optimal"
        }
        Compress-Archive @compress -Force
        
        # Verify and clean up
        if (Test-Path "foundry-mcp-server-${{ env.PACKAGE_VERSION }}.zip") {
          $size = (Get-Item "foundry-mcp-server-${{ env.PACKAGE_VERSION }}.zip").Length / 1KB
          Write-Host "✓ Standalone MCP server ZIP created ($([math]::Round($size, 1)) KB)"
        } else {
          Write-Host "❌ Standalone MCP server ZIP creation failed"
          exit 1
        }
        
        # Clean up temporary directory
        Remove-Item -Recurse -Force $standalonePath

    - name: Verify installer package
      run: |
        cd installer/build
        dir
        if (Test-Path "FoundryMCPServer-Setup-$env:PACKAGE_VERSION.exe") {
          Write-Host "✓ NSIS installer found"
          $size = (Get-Item "FoundryMCPServer-Setup-$env:PACKAGE_VERSION.exe").Length / 1MB
          Write-Host "📊 Installer size: $([math]::Round($size, 1)) MB"
        } else {
          Write-Host "❌ NSIS installer not found"
          exit 1
        }
        
    - name: Upload installer as artifact
      uses: actions/upload-artifact@v6
      with:
        name: foundry-mcp-server-installer-${{ env.PACKAGE_VERSION }}
        path: installer/build/FoundryMCPServer-Setup-${{ env.PACKAGE_VERSION }}.exe
        retention-days: 30

  create-release:
    needs: [build-nsis]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    # Required for softprops/action-gh-release to create the Release + generate notes.
    # Without it the default GITHUB_TOKEN is read-only -> "Resource not accessible by integration" (403).
    permissions:
      contents: write

    steps:
    - name: Checkout code
      uses: actions/checkout@v6

    - name: Setup Node.js
      uses: actions/setup-node@v6
      with:
        node-version: '20.19'

    - name: Set Package Version
      id: version
      run: |
        VERSION="${{ github.ref_name }}"
        # Remove 'v' prefix for filenames if present
        PACKAGE_VERSION="${VERSION#v}"
        echo "PACKAGE_VERSION=v$PACKAGE_VERSION" >> $GITHUB_ENV
        echo "version=v$PACKAGE_VERSION" >> $GITHUB_OUTPUT

    - name: Install workspace dependencies
      run: npm ci --workspaces --include-workspace-root

    - name: Build shared types
      run: npm run build --workspace=shared

    - name: Build MCP Server (Bundled)
      run: npm run build:bundle --workspace=packages/mcp-server

    - name: Build Foundry Module
      run: npm run build --workspace=packages/foundry-module

    - name: Download Windows installer artifact
      uses: actions/download-artifact@v6
      with:
        name: foundry-mcp-server-installer-${{ env.PACKAGE_VERSION }}
        path: installer/build/

    - name: Create Foundry Module ZIP
      # Asset name MUST stay foundry-mcp-bridge.zip — it matches the frozen `download`
      # URL in packages/foundry-module/module.json. Renaming it 404s every Foundry install.
      # Ships only the built module (dist minus sourcemaps/.d.ts + manifest + assets),
      # wrapped in a foundry-mcp-bridge/ folder, matching the proven release.yml packaging.
      run: |
        set -e
        mkdir -p build/foundry-mcp-bridge
        cp packages/foundry-module/module.json build/foundry-mcp-bridge/
        cp -r packages/foundry-module/dist build/foundry-mcp-bridge/
        for d in lang styles templates; do
          if [ -d "packages/foundry-module/$d" ]; then
            cp -r "packages/foundry-module/$d" build/foundry-mcp-bridge/
          fi
        done
        find build/foundry-mcp-bridge/dist -type f \( -name '*.map' -o -name '*.d.ts' \) -delete
        (cd build && zip -r ../foundry-mcp-bridge.zip foundry-mcp-bridge >/dev/null)
        ls -lh foundry-mcp-bridge.zip

    - name: Create Standalone MCP Server ZIP
      run: |
        mkdir -p standalone-mcp-server
        cp packages/mcp-server/dist/index.bundle.cjs standalone-mcp-server/index.js

        # Create package.json
        cat > standalone-mcp-server/package.json << EOF
        {
          "name": "foundry-mcp-server",
          "version": "${{ env.PACKAGE_VERSION }}",
          "description": "Foundry VTT MCP Server - Standalone Installation",
          "main": "index.js",
          "scripts": {
            "start": "node index.js"
          },
          "engines": {
            "node": ">=18.0.0"
          }
        }
        EOF

        # Create README
        cat > standalone-mcp-server/README.md << EOF
        # Foundry MCP Server - Manual Installation

        This package contains the standalone MCP server for manual installation on any platform.

        ## Requirements
        - Node.js 18.0.0 or later
        - Foundry VTT with MCP Bridge module installed

        ## Installation
        1. Extract this ZIP to your desired location
        2. Run: npm start or node index.js
        3. Configure Claude Desktop to connect to this server
        4. Install the MCP Bridge module in Foundry VTT

        For full documentation visit: https://github.com/Gnuminator/Foundry-VTT-MCP-Ai-Tool

        Version: ${{ env.PACKAGE_VERSION }}
        EOF

        zip -r foundry-mcp-server-${{ env.PACKAGE_VERSION }}.zip standalone-mcp-server
        rm -rf standalone-mcp-server
        ls -lh foundry-mcp-server-${{ env.PACKAGE_VERSION }}.zip

    - name: Create GitHub Release
      uses: softprops/action-gh-release@v2
      with:
        tag_name: ${{ github.ref_name }}
        name: Foundry AI Tool ${{ github.ref_name }}
        body: |
          ## Foundry AI Tool ${{ github.ref_name }}

          Complete multi-platform distribution for Foundry MCP Server - AI-powered campaign management for Foundry VTT using Claude Desktop.

          **Compatible with Foundry VTT v13 and v14.**

          ### Installation Options

          **Windows Users:**
          - Download `FoundryMCPServer-Setup-${{ github.ref_name }}.exe`
          - Run installer (no admin rights required)
          - Automatic Claude Desktop configuration

          **Manual Installation:**
          - Download `foundry-mcp-server-${{ github.ref_name }}.zip`
          - Extract and run with Node.js 18+
          - Manual Claude Desktop configuration required

          **Foundry VTT Users:**
          - Install directly from Foundry's module browser
          - Or use manifest: `https://raw.githubusercontent.com/Gnuminator/Foundry-VTT-MCP-Ai-Tool/main/packages/foundry-module/module.json`

          ### Quick Start
          1. Choose your installation method above
          2. Install MCP Bridge module in Foundry VTT
          3. Restart Claude Desktop
          4. Start creating AI-powered campaigns!

          ### Features
          - Comprehensive MCP tool suite for Foundry VTT integration
          - Built for D&D 5e
          - D&D 5e NPC creation suite: stat blocks, attacks, saves, auras, and spellcasting
          - Actor creation with natural language processing
          - Quest management with HTML generation and updates
          - Campaign system with multi-part structure and progress tracking
          - Dice roll coordination between Claude and Foundry players
          - Actor ownership management with bulk operations
          - Enhanced creature index for instant monster searches
          - AI-powered battlemap generation with ComfyUI integration

          For full documentation, visit: https://github.com/Gnuminator/Foundry-VTT-MCP-Ai-Tool
        files: |
          installer/build/FoundryMCPServer-Setup-${{ env.PACKAGE_VERSION }}.exe
          foundry-mcp-bridge.zip
          foundry-mcp-server-${{ env.PACKAGE_VERSION }}.zip
          packages/foundry-module/module.json
        generate_release_notes: true
        draft: false
        prerelease: false

    - name: Update Foundry VTT Package Registry
      # Non-fatal: the GitHub Release (with all artifacts) is already published above, so a
      # missing/invalid FOUNDRY_PACKAGE_RELEASE_TOKEN must not fail the release. Honours the
      # dry_run_foundry dispatch input and skips cleanly when the token secret is absent.
      continue-on-error: true
      env:
        FOUNDRY_TOKEN: ${{ secrets.FOUNDRY_PACKAGE_RELEASE_TOKEN }}
        DRY_RUN: ${{ github.event.inputs.dry_run_foundry }}
      run: |
        echo "🔄 Updating Foundry VTT Package Registry..."

        if [ -z "$FOUNDRY_TOKEN" ]; then
          echo "⚠️  FOUNDRY_PACKAGE_RELEASE_TOKEN is not set — skipping registry update."
          echo "    The GitHub Release is published; submit to the Foundry registry manually if desired."
          exit 0
        fi

        # Extract package information
        PACKAGE_ID=$(node -p "require('./packages/foundry-module/module.json').id")
        VERSION="${{ env.PACKAGE_VERSION }}"
        VERSION="${VERSION#v}"  # Remove v prefix
        MANIFEST_URL="https://github.com/Gnuminator/Foundry-VTT-MCP-Ai-Tool/releases/download/${{ github.ref_name }}/module.json"
        RELEASE_NOTES_URL="https://github.com/Gnuminator/Foundry-VTT-MCP-Ai-Tool/releases/tag/${{ github.ref_name }}"

        MINIMUM=$(node -p "require('./packages/foundry-module/module.json').compatibility.minimum")
        VERIFIED=$(node -p "require('./packages/foundry-module/module.json').compatibility.verified")
        MAXIMUM=$(node -p "require('./packages/foundry-module/module.json').compatibility.maximum")

        echo "📦 Package ID: $PACKAGE_ID"
        echo "📝 Version: $VERSION"
        echo "📎 Manifest URL: $MANIFEST_URL"
        echo "📋 Release Notes: $RELEASE_NOTES_URL"

        # Prepare API request body
        REQUEST_BODY=$(cat <<EOF
        {
          "id": "$PACKAGE_ID",
          "release": {
            "version": "$VERSION",
            "manifest": "$MANIFEST_URL",
            "notes": "$RELEASE_NOTES_URL",
            "compatibility": {
              "minimum": "$MINIMUM",
              "verified": "$VERIFIED",
              "maximum": "$MAXIMUM"
            }
          }
        }
        EOF
        )

        echo "🔧 Request Body:"
        echo "$REQUEST_BODY"

        if [ "$DRY_RUN" = "true" ]; then
          echo "🧪 dry_run_foundry=true — skipping the actual registry POST."
          exit 0
        fi

        # Make API call to Foundry VTT Package Registry
        RESPONSE=$(curl -sS -X POST \
          -H "Content-Type: application/json" \
          -H "Authorization: $FOUNDRY_TOKEN" \
          -d "$REQUEST_BODY" \
          https://foundryvtt.com/_api/packages/release_version/)

        if [ $? -eq 0 ]; then
          echo "✅ Registry API call completed"
          echo "📄 Response: $RESPONSE"
        else
          echo "❌ Failed to reach Foundry VTT Package Registry"
          echo "🔍 Error: $RESPONSE"
          exit 1
        fi
